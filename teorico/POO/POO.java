package teorico.POO;
public class POO {

    /*  Programación Orientada a Objetos: Los objetos como concepto, fuera de la 
     * informática, son cosas que tienen un estado y un comprotamiento. En la
     * programación un objeto es una instancia de una clase que agrupa atributos.
     * Los objetos tienen propiedades (atributos) y comportamientos (métodos) y son
     * unicos.
     * 
     *  Clase: Es una plantilla o molde que defina las propiedades y comportamientos.
     * Esta conformada por 2 partes principales su nombre y el cuerpo (atributos y métodos),
     * para instanciar una clase utilizamos el operador new. 
     *      Atributos: Los atributos son variables que dan caracteristicas a la clase, pueden ser
     *    publicos, privados,
     *      Metodos: Son las funciones que manipulan los atributos o modifican la clase.
     * 
     *  Atributos y metodos estaticos: Un atributo o metodo estadido de la clase
     * no es de la instancia sino que es de la clase y es compartido por todas
     * las instancias de la clase.
     * 
     * Enum: Es una clase especial que representa un grupo de constantes.
     * 
     */
    
}
